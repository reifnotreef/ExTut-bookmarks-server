// const app = require('../src/app');

// describe('App', () => {
//   it('GET / responds with 200 with auth', () => {
//     return supertest(app)
//       .get('/')
//       .set('Authorization', `Bearer ${process.env.API_TOKEN}`)
//       .expect(200);
//   });
//   it('GET / responds with 401 without auth', () => {
//     return supertest(app)
//       .get('/')
//       .expect(401);
//   });
// });
